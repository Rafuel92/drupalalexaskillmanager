const DRUPAL_HOST_NAME = '%hostnameplaceholder%';
const DRUPAL_PROTOCOL = '%protocolplaceholder%';

const config = {
    "hostname" : DRUPAL_HOST_NAME,
    "protocol" : DRUPAL_PROTOCOL
};

module.exports = config;